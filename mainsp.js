/*document.addEventListener("DOMContentLoaded", function() {
    const emailInput = document.getElementById("emailInput");
    const joinButton = document.getElementById("joinButton");
    const notification = document.getElementById("notification");
  
    joinButton.addEventListener("click", function() {
      const email = emailInput.value;
      if (email) {
        // Aquí podrías enviar el correo electrónico a tu servidor o almacenarlo en una base de datos.
        // Simulación de éxito con un mensaje de notificación.
        notification.textContent = "Gracias por unirte a nuestra lista de espera. Te notificaremos pronto.";
        notification.classList.remove("hidden");
        emailInput.value = ""; // Limpia el campo de entrada.
      }
    });
  });
  */